/** The public repository, shared by server and client components (a constant exported from a
 * "use client" module becomes a client reference on the server, not a string). */
export const REPO = "GH-BCooper/INTERACTAI";
